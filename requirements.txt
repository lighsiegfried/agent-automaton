# Core (required for the base API)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
rich>=13.7
python-multipart>=0.0.9    # file uploads for the /voice endpoints

# Testing
pytest>=8.2

# Voice is optional — see requirements-voice.txt (faster-whisper, sounddevice,
# soundfile, numpy, pyttsx3). The API runs fine without it.

# Future / optional — uncomment as the corresponding module is implemented.
# openwakeword>=0.6          # wake word (app/voice/wake_word.py)

# Desktop automation
# pyautogui>=0.9.54          # keyboard/mouse (app/tools/windows_tools.py)
# pywinauto>=0.6.8           # Windows UI automation (app/tools/windows_tools.py)

# Browser automation
# playwright>=1.45           # app/tools/browser_tools.py (also run: playwright install)
